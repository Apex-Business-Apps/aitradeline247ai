project_id = "jbcxceojrztklnvwgyrq"

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://lovable.dev"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# Allow/disallow new user signups to your project.
enable_signup = true

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email addresses. 
# If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false

# Use pre-defined map of claims
#
# [auth.jwt.template]
# claims_json = "{"app_role": "user"}"

[functions.send-lead-email]
verify_jwt = false

[functions.secure-ab-assign]
verify_jwt = false

[functions.secure-analytics]  
verify_jwt = false

[functions.check-password-breach]
verify_jwt = false

[functions.ab-convert]
verify_jwt = false

[functions.voice-answer]
verify_jwt = false

[functions.voice-status]
verify_jwt = false

[functions.track-session-activity]
verify_jwt = true

[functions.chat]
verify_jwt = true

[functions.register-ab-session]
verify_jwt = false

[functions.secure-rate-limit]
verify_jwt = false

[functions.dashboard-summary]
verify_jwt = true

[functions.rag-search]
verify_jwt = true

[functions.rag-answer]
verify_jwt = true

[functions.ragz]
verify_jwt = false

# rag-ingest now requires service_role key - NOT public
# Call with: Authorization: Bearer <SERVICE_ROLE_KEY>