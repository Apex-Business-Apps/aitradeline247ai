name: Wake CI
on:
  push:
    branches: [ "**" ]
    paths-ignore:
      - "**/*.md"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI awake $(date -u)"
